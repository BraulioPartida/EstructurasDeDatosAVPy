You can convert the image into an ASCII art version, which is a text-based representation of the image, and store it inside the .txt file. Tools like catimg or jp2a can convert an image into ASCII that you can then copy into a text file.
